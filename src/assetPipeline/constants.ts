export const THUMB_EDGE = 320;
/** Imports above this edge are downscaled, keeping the original optionally. */
export const MAX_IMAGE_EDGE = 3000;
/** Show progress for anything over this size. */
export const PROGRESS_BYTES = 20 * 1024 * 1024;
/** Warn, but do not block, above this size per file. */
export const WARN_BYTES = 200 * 1024 * 1024;
